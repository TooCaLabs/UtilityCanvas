# ── Web app (Flask server / Render deployment) ─────────────────────────────
Flask>=3.0,<4.0
gunicorn>=22.0,<23.0
gTTS>=2.5,<3.0

# ── Desktop app (app.py — run locally) ─────────────────────────────────────
# sounddevice
# numpy
# faster-whisper
#
# Note: macOS system tools used at runtime (pre-installed on all Macs):
#   /usr/bin/say        — text-to-speech + BlackHole output routing
#   /usr/bin/afconvert  — audio format conversion (Studio tab export)
#   /usr/bin/afplay     — audio playback (Studio tab preview)
#
# Optional virtual microphone (makes Voice Canvas tab visible system-wide):
#   brew install blackhole-2ch
