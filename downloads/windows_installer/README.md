# Windows `.exe` Build (Not Published Yet)

This folder is reserved for the future Windows installer-style release.

- Target output filename: `VocalCanvasSetup.exe`
- Build system: `PyInstaller`
- Publish status: **not exposed on the website yet**

Run `build_windows_exe.bat` on a Windows machine to generate the `.exe`.
